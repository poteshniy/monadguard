node_modules/
data/
.env
.env.*
!.env.example
*.log
logs/
# deployment.json IS committed on purpose — the deployed address is public
# information and judges need it. Never put keys in it.
*.bak
.*.bak
port/
*.pre-cleanup
SUBMISSION_COPY.md
# Only the output directory at the repo root — NOT scripts/capture/, which holds
# the probe and the target list. A bare `capture/` matches both.
/capture/
